import React from 'react';

// This component is obsolete now that e-signature functionality is integrated elsewhere.
// It is no longer used in the application and can be safely deleted.
const PublicSignatureVerifier: React.FC = () => null;

export default PublicSignatureVerifier;